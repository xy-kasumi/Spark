{
  "name": "shell-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Shell dashboard for Spark",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tsc && cp index.html logo.png dist/ && cp node_modules/vue/dist/vue.esm-browser.js node_modules/chart.js/dist/chart.umd.js dist/",
    "dev": "nodemon --watch . --ignore dist --ignore node_modules --ext ts,html --exec 'npm run build'"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "chart.js": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "nodemon": "^3.0.0"
  }
}